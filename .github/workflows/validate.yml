name: Validate

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static:
    name: Static checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Validate Python and JSON
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 - <<'PY'
          import ast
          import json
          from pathlib import Path

          errors = []
          ignored = {".git", "node_modules", "dist"}

          for path in Path(".").rglob("*"):
              if not path.is_file() or ignored.intersection(path.parts):
                  continue
              try:
                  source = path.read_text(encoding="utf-8")
              except UnicodeDecodeError:
                  continue
              first_line = source.partition("\n")[0]
              if first_line.startswith("#!") and "python" in first_line:
                  try:
                      ast.parse(source, filename=str(path))
                  except SyntaxError as exc:
                      errors.append(f"{path}: {exc}")

          for path in Path(".").rglob("*.json"):
              if ignored.intersection(path.parts):
                  continue
              try:
                  with path.open(encoding="utf-8") as stream:
                      json.load(stream)
              except (OSError, json.JSONDecodeError) as exc:
                  errors.append(f"{path}: {exc}")

          if errors:
              print("\n".join(errors))
              raise SystemExit(1)
          PY

      - name: Validate shell scripts and systemd state paths
        shell: bash
        run: |
          set -Eeuo pipefail
          while IFS= read -r -d '' file; do
            if head -n 1 "$file" | grep -Eq '^#!.*(bash|/sh)([[:space:]]|$)'; then
              bash -n "$file"
            fi
          done < <(find . -type f \
            -not -path './.git/*' \
            -not -path './ui/node_modules/*' \
            -print0)

          if grep -R --line-number --fixed-string '%S/state' imageroot; then
            echo "Legacy %S systemd state path found; use %E on current NS8 systems." >&2
            exit 1
          fi

      - name: Check module wiring
        shell: bash
        run: |
          set -Eeuo pipefail
          grep -Fq 'org.nethserver.tcp-ports-demand=1' build-images.sh
          grep -Fq 'org.nethserver.rootfull=0' build-images.sh
          grep -Fq '${ORGANIZR_IMAGE}' imageroot/systemd/user/organizr.service
          grep -Fq 'volumes/organizr-app' imageroot/etc/state-include.conf

  ui:
    name: UI lint and build
    runs-on: ubuntu-latest
    env:
      NODE_OPTIONS: --openssl-legacy-provider
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Enable Corepack
        run: corepack enable

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24.20.0"
          cache: yarn
          cache-dependency-path: ui/yarn.lock

      - name: Install dependencies
        run: yarn install --immutable
        working-directory: ui

      - name: Lint
        run: yarn lint
        working-directory: ui

      - name: Build
        run: yarn build
        working-directory: ui
